project('boost', 'cpp',
        version: '1.87.0')

inc_dir = include_directories('.')

boost = library('boost', include_directories : inc_dir)
boost_dep = declare_dependency(include_directories : inc_dir)

